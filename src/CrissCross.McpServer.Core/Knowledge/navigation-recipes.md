# Navigation recipes

Use `NavigationRegistry`/`IBidirectionalNavigator` for navigation-only flows.
Use `IViewModelRoutedViewHost` with `SetMainNavigationHost` for hosted VM navigation.
Use `INavigationService`/`IPageService` for page navigation surfaces.
